name: business_logic
description: A package serving special FHIR types to work with an OpenIMIS backend
version: 1.0.1
homepage: https://git.informatik.hs-mannheim.de/openimis/openimis
publish_to: none

environment:
  sdk: '>=2.16.2 <3.0.0'

dependencies:
  fhir_openimis:
    path: ../fhir_openimis
  fhir: ^0.6.0
  shared_preferences: "2.0.15"
  sqflite:
  path:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.21.0
